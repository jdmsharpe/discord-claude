# Discord Claude Bot - Developer Reference

## Quick Start

```bash
uv sync --extra dev                   # creates .venv from uv.lock (no pip inside — use `uv pip` if needed)
cp .env.example .env                  # then fill in required values
git config core.hooksPath .githooks   # enable repo pre-commit hook
uv run python src/bot.py   # or: docker compose up --build
```

## Gotchas

- Uses **`py-cord`** (not `discord.py`). The slash-command API differs; don't mix docs between the two.
- `GUILD_IDS` must list at least one guild ID. Empty or unset parses to `[]`, and py-cord only registers a command globally when its `guild_ids is None`, so an empty list registers the commands **nowhere** — not globally, not per-guild. `validate_required_config()` does not check it, so the bot starts clean with no commands.

## Environment Variables

| Variable | Required | Description |
| --- | --- | --- |
| `BOT_TOKEN` | Yes | Discord bot token |
| `GUILD_IDS` | Yes | Comma-separated Discord server IDs; at least one is required — empty or unset registers no commands at all (not enforced at startup) |
| `ANTHROPIC_API_KEY` | Yes | Anthropic API key |
| `SHOW_COST_EMBEDS` | No | Show cost embeds (`true`/`1`/`yes`, default: `true`) |
| `MEMORIES_DIR` | No | Per-user memory directory (default: `./memories`) |
| `ANTHROPIC_MCP_PRESETS_JSON` | No | Inline JSON of named MCP presets |
| `ANTHROPIC_MCP_PRESETS_PATH` | No | Path to JSON file of named MCP presets |
| `CLAUDE_PRICING_PATH` | No | Override the bundled `src/discord_claude/config/pricing.yaml` |
| `LOG_FORMAT` | No | `text` (default) or `json` for structured JSON-lines output |

## Supported Entry Points

- Launcher: `python src/bot.py` remains supported and delegates to `discord_claude.bot.main`.
- Cog composition contract:

  ```python
  from discord_claude import ClaudeCog

  bot.add_cog(ClaudeCog(bot=bot))
  ```

  `ClaudeCog.__init__` calls `validate_required_config()` automatically, so config is validated at cog construction regardless of whether `main()` is used. Note: `GUILD_IDS` format is validated at import time (it is captured at class-definition time by `SlashCommandGroup`); `BOT_TOKEN` and `ANTHROPIC_API_KEY` missing or blank values are validated at construction time.
- `discord_claude` and `discord_claude.cogs.claude` both use lazy `__getattr__` exports so helper imports do not eagerly pull in Discord-heavy modules. Type-only imports keep `pyright src/` aware of those public exports.

## Package Layout

```text
src/
├── bot.py                           # Thin repo-local launcher
└── discord_claude/
    ├── __init__.py
    ├── bot.py
    ├── logging_setup.py             # Structured logging + request-id ContextVar
    ├── memory.py
    ├── util.py
    ├── config/
    │   ├── __init__.py
    │   ├── auth.py
    │   ├── mcp.py
    │   ├── pricing.py                # YAML loader exposing MODEL_PRICING, MODEL_CONTEXT_WINDOWS, etc.
    │   └── pricing.yaml              # Canonical pricing data (override via CLAUDE_PRICING_PATH)
    └── cogs/
        ├── __init__.py
        └── claude/
            ├── __init__.py
            ├── attachments.py
            ├── chat.py
            ├── client.py
            ├── cog.py
            ├── command_options.py    # OptionChoice lists for slash-command menus
            ├── embed_delivery.py     # Discord embed batching (6000-char/10-embed caps)
            ├── embeds.py
            ├── models.py
            ├── paths.py
            ├── responses.py
            ├── state.py
            ├── tool_handlers.py
            ├── tool_registry.py
            └── views.py
```

Only `src/bot.py` remains at the repo root; code imports should target `discord_claude...`.

## Testing And Patch Targets

- `pytest` runs with `pythonpath = ["src"]`.
- The test suite is organized into module-aligned files such as `tests/test_claude_cog.py`, `tests/test_claude_chat.py`, `tests/test_claude_client.py`, `tests/test_claude_tool_handlers.py`, `tests/test_config_auth.py`, `tests/test_tool_registry.py`, `tests/test_lazy_imports.py`, `tests/test_claude_embeds.py`, `tests/test_claude_responses.py`, `tests/test_button_view.py`, `tests/test_memory.py`, and `tests/test_util.py`.
- MCP-specific coverage lives primarily in `tests/test_claude_mcp_config.py`, `tests/test_claude_request_config.py`, and the MCP cases in `tests/test_claude_chat.py`.
- `tests/test_package_import.py` is the package import smoke test.
- New tests and patches should target real owners under `discord_claude...`.
- Examples:
  - `discord_claude.memory.get_memories_base_dir`
  - `discord_claude.cogs.claude.attachments.SUPPORTED_IMAGE_TYPES`
  - `discord_claude.cogs.claude.client.AsyncAnthropic`
  - `discord_claude.cogs.claude.chat.call_api_with_tool_loop`
  - `discord_claude.config.mcp.ANTHROPIC_MCP_PRESETS`
  - `discord_claude.cogs.claude.chat.build_api_params`
  - `discord_claude.cogs.claude.tool_registry.TOOL_REGISTRY`
- Import `ClaudeCog` from `discord_claude`; do not reintroduce legacy `anthropic_api` shim paths.
- Auth config tests (`tests/test_config_auth.py`) use `importlib` to force a fresh module import per test. Because `load_dotenv()` runs at import time and restores values from the `.env` file, tests that control env state must suppress it: `monkeypatch.setattr("dotenv.load_dotenv", lambda *_, **__: None)`.

## Validation Commands

```bash
ruff check src/ tests/
ruff format src/ tests/
pyright src/
pytest -q
```

- The repo pre-commit hook (`.githooks/pre-commit`) runs `ruff format` (auto-applied + re-staged), then `ruff check` (blocking), then `pyright` and `pytest --collect-only` as warning-only smoke tests. Resolves tools from `.venv/bin` or `.venv/Scripts` first, then `PATH`.

## Provider Notes

- Memory storage root is resolved via `discord_claude.cogs.claude.paths`.
- Tool metadata (Anthropic payload, UI label/description, execution mode) lives in `discord_claude.cogs.claude.tool_registry.TOOL_REGISTRY`. Adding a tool requires only a new `ToolRegistryEntry` there; `get_anthropic_tools` and `get_tool_select_options` derive the API and UI views automatically.
- Client-side tool dispatch lives in `ClaudeCog._execute_tool`, which looks up handlers in the per-cog `_tool_handlers` dict (initialized from `default_tool_handlers()` in `discord_claude.cogs.claude.tool_handlers`). The `memory` handler calls through `discord_claude.memory` so tests can patch the live owner. Use `cog.register_tool_handler(name, handler)` / `cog.unregister_tool_handler(name)` to extend or replace handlers at runtime.
- Conversations remain keyed by `(user_id, channel_id)`.
- Named MCP presets are loaded from `ANTHROPIC_MCP_PRESETS_JSON` (inline JSON) and/or `ANTHROPIC_MCP_PRESETS_PATH` (path to a JSON file); both are additive and duplicate preset names across them are rejected at startup.
- Claude MCP presets support only `url` (HTTPS), `authorization_env_var` (user-defined runtime token env var), `allowed_tools`, and `defer_loading` — no `kind` discriminator or approval loop.
- MCP state persists independently from built-in tool names via `mcp_preset_names` on `ChatCompletionParameters`.
- Anthropic MCP traffic is passed through with `mcp_servers` and `mcp_toolset`, and `call_api_with_tool_loop` adds the `mcp-client-2025-11-20` beta when MCP is active.
- MCP content blocks are ignored by the local tool loop; only built-in Anthropic tool calls are executed client-side.
- **Future migration target:** the SDK now ships `client.beta.messages.tool_runner` with `anthropic.lib.tools.mcp.async_mcp_tool` for first-class MCP integration. Once that surface is GA-promoted out of `beta`, retire the manual `mcp-client-2025-11-20` beta-header path in `call_api_with_tool_loop`. (Last checked anthropic 1.8.0 on 2026-09-22 — still beta-only: `client.messages.tool_runner` does not exist, only `client.beta.messages.tool_runner`; `anthropic.lib.tools.mcp` additionally requires the `anthropic[mcp]` extra. Recheck on next anthropic SDK minor bump.)
- **Refusal fallback (server-side, beta):** when the requested model is in `REFUSAL_FALLBACK_MODELS` (`util.py`: `claude-fable-5-1`, `claude-fable-5`, `claude-opus-5-5` and `claude-opus-5` — the models Anthropic's refusals page and the Fable 5.1 launch notes say carry safety classifiers, verified 2026-09-03; Opus 5.5 verified 2026-09-22, same permitted targets Opus 4.8 / Opus 5), `call_api_with_tool_loop` adds the `server-side-fallback-2026-06-01` beta and `fallbacks=[{"model": REFUSAL_FALLBACK_MODEL}]` (`claude-opus-4-8`; the explicit-list form accepts up to three named models, and a `fallbacks="default"` routing mode exists under the `server-side-fallback-2026-07-01` header — not adopted). Served-by detection reads `usage.iterations` for a `fallback_message` entry (covers sticky-routed turns that carry no `fallback` content block) and stamps `ParsedResponse.served_model`; `append_fallback_embed` surfaces a notice, and `extract_response_content` skips the `fallback` block type. **Billing (2026-09-22):** `UsageTotals.accumulate` groups every `message` / `fallback_message` entry under the model it names (`tokens_by_model`, stamped onto `ParsedResponse.tokens_by_model`; the `None` key is the request model — top-level usage, `compaction` entries, entries without a `model`) and `track_daily_cost` bills each group at its own rates via `priced_model` (an id without a pricing row, such as a dated snapshot id, resolves to the request model instead of the $15/$75 unknown-model fallback). Before, the whole fallback turn, the declined attempt included, was billed at the served model's rates. Known gap: the refusals docs ("Billing and rate limits") say an attempt that declined before producing any output is not billed, but in anthropic 1.8.0 the declined hop is an ordinary `message` entry (`BetaMessageIterationUsage`: `type`, optional `model`, token counts — no stop or refusal marker), so the bot cannot identify it and bills it at the declined model's rates. Pinned by `TestTrackDailyCost` (`tests/test_claude_state.py`), `test_refusal_fallback_turn_bills_each_attempt_at_its_models_rates` (`tests/test_claude_chat.py`) and `test_accumulate_fallback_message_iterations` (`tests/test_util.py`). The alternative client-side `BetaRefusalFallbackMiddleware` was deliberately not used — the bot is first-party-API only, so the one-round-trip server-side path is simpler.
- **Per-model effort gate:** `output_config.effort` support differs by model and the API returns a 400 for unsupported combinations. `util.py` pins it in three frozensets — `EFFORT_MODELS` (every selectable model except `claude-sonnet-4-5` / `claude-haiku-4-5`, which reject the parameter outright), `XHIGH_EFFORT_MODELS` (Fable 5.1, Fable 5, Opus 5.5, Opus 5, Opus 4.8, Opus 4.7, Sonnet 5 — the full `low`..`max` ladder), and `MAX_EFFORT_MODELS` (`XHIGH_EFFORT_MODELS` plus Opus 4.6 / Sonnet 4.6, which take `max` but not `xhigh`); `claude-opus-4-5` is in `EFFORT_MODELS` only (capped at `high`). `supported_effort_levels(model)` derives the accepted set and `validate_request_configuration` rejects anything else before dispatch with a friendly error listing the accepted levels in `EFFORT_LEVELS` order. `RESPONSE_EFFORT_CHOICES` still offers all five levels because Discord slash-option choices cannot vary per model. Pinned by `tests/test_util.py::TestModelCapabilitySets::test_effort_model_sets_membership` and `::test_every_chat_model_choice_has_an_effort_classification` (which requires every `CHAT_MODEL_CHOICES` id to be classified) plus the `test_validate_request_configuration_*_effort_*` cases in `tests/test_claude_request_config.py`. Live-probed 2026-08-28 (Fable 5.1 on 2026-09-03, Opus 5.5 on 2026-09-22); re-probe when a model is added. The `effort` option defaults to not set, so the model's own default applies: `medium` on Opus 5.5, where Opus 5 ran at `high`.
- **Programmatic-tool-calling gate (v1.12.3):** `web_search_20260209` and `web_fetch_20260309` default `allowed_callers` to the code-execution caller, and `claude-haiku-4-5` lacks programmatic tool calling, so it 400s on that default (`does not support programmatic tool calling ... Explicitly set allowed_callers=["direct"]`) — every `/claude chat` on Haiku with a web tool toggled failed before the fix. `PROGRAMMATIC_TOOL_CALLING_UNSUPPORTED_MODELS` (`util.py`) lists it and `build_api_params` (`chat.py`) copies the web tools with `allowed_callers: ["direct"]` for those models only (the registry dicts are not mutated). Probed 2026-09-15: every other menu model accepts the default (Opus 5.5 supports programmatic tool calling too, 2026-09-22); Opus 5 with `direct` also works, at ~2k fewer input tokens because no code-execution caller is provisioned; not applied there. Pinned by `TestProgrammaticToolCallingGate` in `tests/test_claude_request_config.py`.
- **Code-execution tool version (2026-09-22):** `TOOL_REGISTRY["code_execution"]` sends `code_execution_20260521`. With `code_execution_20250825` in the same request as `web_search_20260209` or `web_fetch_20260309`, every menu model except Haiku 4.5 returned 400 `Auto-injecting tools would conflict with existing tool names: ['code_execution']` (live-confirmed on Opus 5): with their default `allowed_callers` the web tools make the API add its own `code_execution` tool, and next to these web tool versions the API requires `code_execution_20260120` or later. Haiku 4.5 was unaffected because it sends the web tools with `allowed_callers: ["direct"]`. `code_execution_20260521` returned 200 on Opus 5, Opus 5.5 and Haiku 4.5 (direct-caller web tools). So before this fix, `/claude chat` with code execution plus web search or web fetch failed on every model except Haiku. When a web tool version changes, check which code-execution version it requires. Pinned by `tests/test_tool_registry.py::TestToolRegistry::test_tool_versions`.
- **Server-side compaction:** `COMPACTION_MODELS` (`util.py`: Fable 5.1, Fable 5, Opus 5.5, Opus 5, Sonnet 5, Opus 4.8 / 4.7 / 4.6, Sonnet 4.6) opt into the `compact-2026-01-12` beta with a `compact_20260112` edit in `call_api_with_tool_loop`. Every other selectable model (Opus 4.5, Sonnet 4.5, Haiku 4.5 — all 200k) takes the manual path: once the full prompt of the most recent response (`UsageTotals.prompt_tokens` = `input_tokens` + `cache_read_input_tokens` + `cache_creation_input_tokens`, read from the last `message` / `fallback_message` iteration when `usage.iterations` is present) passes `manual_compaction_trigger(context_window)` — `CONTEXT_COMPACTION_THRESHOLD` (75%) of the smaller of the model window and the `COMPACTION_SUMMARY_MODEL` (Haiku 4.5, 200k) window — `compact_conversation` replaces the history with a Haiku-written summary. That `min()` is currently a no-op guard (no manual-path model exceeds 200k; Opus 5 was the only 1M model on that path before it joined `COMPACTION_MODELS`) but stays so a future 1M manual-path model cannot hand the summarizer a 750k payload. Pinned by `tests/test_util.py::TestModelCapabilitySets::test_opus_5_takes_the_server_side_compaction_path` (Opus 5 membership plus every manual-path choice fitting the summarizer window) and `::test_manual_compaction_trigger_bounded_by_summarizer`, plus `tests/test_claude_chat.py::TestRunChatCommand::test_opus_5_uses_server_side_compaction` (Opus 5 and Opus 5.5: beta path, no summarizer pass past the 150k manual trigger). `compact_if_over_trigger` in `call_api_with_tool_loop` checks before each follow-up request of the tool loop and once more after the final response, so the next turn starts from `[user(summary)]` and its user message follows as a second consecutive user message, which the API merges. **Trigger fix (2026-09-22):** the check used to compare `totals.input_tokens`, a per-turn sum of uncached input, and ran only before follow-up requests; a plain chat turn (one response) never compacted, and the top-level 1h `cache_control` reports most of a long history as cache reads anyway. `context_warning` uses the same `prompt_tokens` measure and is not raised after a manual compaction (`prompt_tokens` is reset because the history was replaced). The summarizer call (`messages.parse`) is now billed: `compact_conversation(..., usage_totals=totals)` adds its usage through `UsageTotals.accumulate_compaction_summary` under `COMPACTION_SUMMARY_MODEL`, billed at Haiku 4.5 rates and included in the cost embed's token counts; before, it was discarded. Pinned by `test_manual_compaction_counts_cached_prompt_tokens`, `test_manual_compaction_uses_the_latest_prompt_not_the_turn_total` and `test_manual_compaction_bills_the_summarizer_call` in `tests/test_claude_chat.py` (the first two fail against the old trigger), and `test_prompt_tokens_is_the_full_prompt_of_the_latest_response` / `test_accumulate_compaction_summary_bills_at_the_summary_model` in `tests/test_util.py`. Compaction docs verified 2026-09-03 (Fable 5.1 listed; on it, thinking blocks before a `compaction` block are not carried forward, so the summary is all it keeps of that work). **Billing (v1.12.3):** the summarisation call is billed at the request model's rates and excluded from the top-level `input_tokens` / `output_tokens` — it appears as a `{"type": "compaction"}` entry in `usage.iterations` (compaction docs, "Understanding usage") — so `UsageTotals.accumulate` (`util.py`) sums those entries into the executor totals and sets `context_compacted` (the compaction embed now shows for server-side compaction too); before the fix every server compaction was unbilled in the cost embed. anthropic 1.6.0 also added an on-demand top-level `compaction` parameter (`compact-2026-09-04` beta; returns a signed `compaction` block) — it cannot be combined with `context_management` on one request, so the threshold `compact_20260112` edit stays and the new parameter is not adopted. Pinned by `test_accumulate_compaction_iterations` in `tests/test_util.py`.
- **Advisor pairs:** `ADVISOR_MODEL_COMPATIBILITY` (`util.py`) holds the executor -> advisor table on the advisor-tool docs page plus `claude-opus-5-5`, which the API accepts but the table does not list (docs table verified 2026-09-03; `ADVISOR_BETA` `advisor-tool-2026-03-01` and `ADVISOR_TOOL_TYPE` `advisor_20260301` unchanged). The `advisor` slash option is a bare toggle, so `get_default_advisor_model` (first tuple entry) is the only selection surface and tuple order is load-bearing: `claude-opus-4-8` leads every tuple that allows it because it returns plaintext advice; Opus 5 / Fable 5 executors accept only Opus 5 / Opus 5.5 / Fable 5 / Fable 5.1 advisors (default `claude-opus-5`), an Opus 5.5 executor the same four (default `claude-opus-5-5`), and a Fable 5.1 executor only a Fable 5.1 advisor; the Opus 5 / Fable 5 / Fable 5.1 advisors return an encrypted `advisor_redacted_result`. No extra handling is needed for that: `extract_response_content` (`responses.py`) and `_copy_messages_without_advisor_blocks` (`state.py`) filter on the outer `advisor_tool_result` block type, and the tool loop replays `response.content` verbatim. `claude-mythos-5` / `claude-mythos-5-1` are in the docs table but not publicly callable, so they are omitted; `claude-sonnet-4-5` / `claude-opus-4-5` are not executors and `validate_request_configuration` keeps rejecting them. **Opus 5.5 (2026-09-22):** the docs table does not list `claude-opus-5-5`, but the API accepts it. Checked with `beta.messages.count_tokens` and the advisor tool: every executor with a table entry except Fable 5.1 accepted exactly its documented advisors plus `claude-opus-5-5` (a Fable 5.1 executor accepted only Fable 5.1), and an Opus 5.5 executor accepted Opus 5.5 / Opus 5 / Fable 5 / Fable 5.1 advisors and rejected Opus 4.8 (400 "'claude-opus-4-8' cannot be used as an advisor when the request model is 'claude-opus-5-5'"). The same check reproduced the documented rejections (e.g. Opus 5 executor + Opus 4.8 advisor → 400), and a real `messages.create` with Opus 5.5 as executor and advisor ended `end_turn` with `server_tool_use`, `advisor_tool_result` and `text` blocks, so the advisor ran. `claude-opus-5-5` sits directly after `claude-opus-5` in each existing tuple except Fable 5.1's, so no tuple's first entry and no existing default changed; the Opus 5.5 executor entry leads with `claude-opus-5-5`, the cheapest advisor it accepts, so the default model has an advisor. Whether an Opus 5.5 advisor returns plaintext or `advisor_redacted_result` was not recorded; the bot handles both. Pinned by `tests/test_util.py::TestModelCapabilitySets::test_advisor_model_compatibility_matches_accepted_pairs` / `::test_default_advisor_model_prefers_plaintext_opus_4_8`, `tests/test_claude_cog.py::test_default_chat_model_has_an_advisor`, and the `*_advisor_*` cases in `tests/test_claude_request_config.py` (every listed pair accepted, off-table pairs — including Opus 5.5 executor + Opus 4.8 — and non-executors rejected).

- **Claude Opus 5.5 (`claude-opus-5-5`, GA 2026-09-22; the `/claude chat` default):** $4 / $20 per MTok; cache reads $0.20 (0.05x, declared as `cache_read_per_million` in `pricing.yaml`, not the derived 0.1x); 1h cache writes 2x ($8), which matches the calculator's write rule; 1M context, 128K max output; in the anthropic 1.8.0 `Model` literal. Live-probed 2026-09-22: adaptive thinking only (`enabled` and `disabled` both 400), sampling-locked (`temperature` / `top_p` / `top_k` 400), all five effort levels, forced `tool_choice` (`any` / `tool`) 400s, server-side compaction (`compact_20260112`), per-message effort, `thinking.display: "updates"`, refusal classifiers (server-side fallback targets Opus 4.8 and Opus 5, so `REFUSAL_FALLBACK_MODEL` Opus 4.8 is valid), programmatic tool calling with the default `allowed_callers`, and the advisor tool as executor and as advisor although the advisor docs table lists neither (see Advisor pairs). An omitted `effort` runs at `medium` (Opus 5: `high`). A messages request with the bot's full shape (per-message effort, context management, compaction, server-side fallback with `fallbacks=[opus-4-8]`, top-level 1h `cache_control`, adaptive thinking, `web_search_20260209` + `web_fetch_20260309` + `code_execution_20260521`) returned 200. Set memberships are pinned by `tests/test_util.py::TestModelCapabilitySets::test_opus_5_5_capability_membership`, pricing by `TestCalculateCost::test_opus_5_5_pricing` and `tests/test_config_pricing.py`, the default by `tests/test_claude_cog.py::test_chat_command_default_model` and the option-description guard.
- **Claude Fable 5.1 (`claude-fable-5-1`, GA 2026-09-01; menu option, not the default):** same $10/$50 as Fable 5 but cache reads bill at 0.025x ($0.25/MTok), declared as `cache_read_per_million` in `pricing.yaml`, loaded into `CACHE_READ_PRICING` and read by `calculate_cost` (every other row except Opus 5.5 keeps the derived 0.1x; pinned by `test_fable_5_1_cache_reads_bill_at_the_declared_quarter_rate` in `tests/test_util.py` and `test_cache_read_rate_is_declared_only_where_it_departs_from_the_default` in `tests/test_config_pricing.py`). Adaptive-only and sampling-locked like Fable 5, and it additionally 400s on forced tool use (`tool_choice` `any` / `tool`): `FORCED_TOOL_CHOICE_UNSUPPORTED_MODELS` + `validate_request_configuration` refuse it with a model-specific message ahead of the generic thinking rule (all live-probed 2026-09-03; `test_validate_request_configuration_rejects_forced_tool_use`, which covers Opus 5.5 too). Its thinking blocks are readable only by Fable 5.1 or newer — a later turn on another model silently drops them — and, for accounts created on or after 2026-08-31, the API 400s when anything before a Fable 5.1 thinking block changes between requests (`system`, `tools`, an earlier message); this account predates that cutoff, so the mismatch is recorded rather than enforced, but keep the replayed history append-only regardless. Of the betas it introduced, per-message effort and `thinking.display: "updates"` are adopted (v1.12.0, bullets below); turn-scoped system messages are not.
- **Per-message effort (v1.12.0, `/claude effort`):** `run_effort_command` → `apply_effort_change(conversation, level)` (`chat.py`) changes the effort of the caller's active conversation for its following turns after the same `supported_effort_levels` gate as `/claude chat`. On `PER_MESSAGE_EFFORT_MODELS` (`util.py`: Fable 5.1, Opus 5.5, Opus 5) it appends an effort-only `{"role": "system", "content": [], "output_config": {"effort": level}}` message to the history (a trailing one is replaced, so consecutive changes do not stack) and does not change the top-level `output_config.effort`, so the cached prompt prefix stays valid; `call_api_with_tool_loop` sends the `mid-conversation-output-config-2026-07-01` beta (`PER_MESSAGE_EFFORT_BETA`) on EVERY request for these models, not only once an override exists (v1.12.1): in the Discord run the turn after `/claude effort` showed no cached tokens and a ~1.6k-token cache write, and an API probe with the bot's exact shape reproduced it — the header added mid-conversation re-renders the prompt (override turn: write 2,366 / read 0), while the same header sent from turn 1 keeps the read (override turn: read 2,349 / write 19; control without any change: read 2,347). The system message itself does not break the prefix. Every other model gets `params.effort` changed instead (the reply says the cache is invalidated). Live-probed 2026-09-03: Opus 5 and Fable 5.1 accept the override (two consecutive ones too); Fable 5 400s "output_config.effort requires a model that supports per-turn effort", and without the header every model 400s "messages.N.output_config: Extra inputs are not permitted". Manual compaction never processes these messages (every per-message model is in `COMPACTION_MODELS`); `current_effort(conversation)` reports the effective level. Pinned by `TestEffortChange`, `test_effort_override_message_sends_per_message_effort_beta`, `test_per_message_effort_models_send_the_beta_from_the_first_request` and `test_models_without_per_message_effort_do_not_send_the_beta` in `tests/test_claude_chat.py`, `test_effort_command_offers_the_full_effort_ladder` in `tests/test_claude_cog.py`.
- **Thinking display (v1.12.0, `/claude chat thinking_display`):** `ChatCompletionParameters.thinking_display` (`summarized` default, `THINKING_DISPLAY_CHOICES`) is used as the adaptive `display` value in `build_thinking_config`. `updates` is gated by `validate_request_configuration` to `THINKING_DISPLAY_UPDATES_MODELS` (Fable 5.1, Fable 5, Opus 5.5 — the models that write progress updates between tool calls; Opus 5 accepts the value but returns an empty thinking block, probed 2026-09-03; Opus 5.5 supports `updates`, 2026-09-22). Under `updates`, `call_api_with_tool_loop` adds the `thinking-display-updates-2026-08-18` beta and, on every `tool_use` iteration, passes the response's readable text (text blocks plus any non-empty thinking block — reasoning comes back empty in this mode) to the `progress_callback` that `make_progress_poster` creates from `ctx.send_followup` / `message.channel.send`, so users see the status lines while the tools run instead of only the typing indicator; the final spoilered thinking embed is empty in this mode. Probe note 2026-09-03: in a four-tool loop Fable 5.1 emitted its progress lines as *text* blocks before each `tool_use` under both displays, and the single-shot `summarized` probe returned a reasoning summary in the thinking block where `updates` returned it empty — so the callback covers both block types. Pinned by `test_updates_display_posts_progress_and_sends_beta` / `test_summarized_display_posts_no_progress` (`tests/test_claude_chat.py`), `TestThinkingDisplay` (`tests/test_claude_request_config.py`), `test_thinking_display_choice_set` (`tests/test_claude_cog.py`); the option-description guard count is now 2.

## Runtime Conventions (Cross-Project)

- **Pricing** is loaded from `src/discord_claude/config/pricing.yaml` by `config/pricing.py` at import time. Override via `CLAUDE_PRICING_PATH` to push a vendor price change without a code release. Cross-referenced against [genai-prices/anthropic.yml](https://github.com/pydantic/genai-prices/blob/main/prices/providers/anthropic.yml).
- **Retry**: the `AsyncAnthropic` client is built with `max_retries=4, timeout=300` (total 5 attempts) in `client.py`; transient 429/5xx/connection errors recover transparently via the Anthropic SDK's built-in exponential backoff.
- **Conversation TTL**: `prune_runtime_state` in `cogs/claude/state.py` evicts conversations older than `CONVERSATION_TTL` (12h) every 15 minutes via `@tasks.loop`. Caps at `MAX_ACTIVE_CONVERSATIONS`. Daily costs retained for `DAILY_COST_RETENTION_DAYS` (30).
- **Request IDs**: `cog_before_invoke` (and `on_message`) bind a fresh 8-char hex id via `discord_claude.logging_setup.bind_request_id`. All downstream `logger.info`/`warning`/`error` calls automatically include the id. Set `LOG_FORMAT=json` for JSON-lines output.
- **Async file I/O**: blocking `open()` and `pathlib` methods (`read_bytes`, `write_bytes`, `unlink`, etc.) inside `async def` freeze the Discord event loop and stall every concurrent slash command. Wrap them with `asyncio.to_thread(...)` so the I/O runs on a worker thread. Enforced by `ruff` (`ASYNC230`/`ASYNC240`).
